// Task-11 stub — will be replaced with the full MeasureForm implementation.
import type { FormProps } from '../home/forms/formKit'
import type { MeasurementType } from '../../db/types'

export function MeasureForm({ initialType: _initialType }: FormProps & { initialType: MeasurementType }) {
  return null
}
